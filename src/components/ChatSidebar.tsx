import React from "react";

// This component is now integrated into ChatInterface
// Keeping this file for backward compatibility but it's no longer used
const ChatSidebar: React.FC<any> = () => {
  return null;
};

export default ChatSidebar;
